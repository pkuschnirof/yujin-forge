# %%PROJECT_NAME%%

Generated by [Yujin Forge](https://github.com/pkuschnirof/yujin-forge).
React + Vite + NAC-3 by construction.

## Develop

```bash
npm install
npm run dev          # vite at http://localhost:5173
```

## Test

```bash
npm test             # vitest unit
npm run test:e2e     # playwright e2e
```

## Ship

```bash
npm run build        # tsc + vite build -> dist/
yf ship              # forge gate (tests + nac validate + license)
```

## What Forge gave you

- `src/nac/manifest.ts` -- NAC-3 manifest pre-wired.
- `data-nac-*` attrs on every interactive element.
- `yujin.forge.json` -- project config + license ref.
- Starter Playwright + Vitest setup (test catalogue
  expands as you add components via `yf chat`).

## Add a feature

```bash
yf chat
> Add a "priority" column to the todos list, with low / med / high.
```

The chat panel proposes the diff; you approve; Forge writes
the component + the manifest entry + the tests.
